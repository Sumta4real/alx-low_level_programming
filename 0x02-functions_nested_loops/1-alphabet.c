#include <stdio.h>

/**
 * main - This file prints all alphabets in lower case
 * Return: 0 if success
 */

int main()
{       
        char ch = 'a';
        for (ch='a'; ch <='z'; ch++)
        {
                _putchar(ch);
        }
        _putchar('\n');
}       
